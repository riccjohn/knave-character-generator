import styled from "styled-components"

const StyledHitpoints = styled.div`
  font-family: SebaldusGotisch;
  font-size: 1.1em;
  padding: 0 1em;
`

StyledHitpoints.displayName = "StyledHitpoints"

export default StyledHitpoints
